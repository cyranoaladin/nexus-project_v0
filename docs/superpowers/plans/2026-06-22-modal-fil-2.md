# Modal Fil 2 Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Extend the authenticated modal proof suite from the six fil 1 modals to every remaining mounted dashboard modal that is reachable with real auth.

**Architecture:** Keep one canonical Playwright helper battery in `e2e/auth/dialog-all-roles-proof.spec.ts`. Seed only deterministic e2e data required for real UI triggers in `scripts/seed-e2e-db.ts`; fix modal a11y/charte defects in the mounted page/component instead of weakening assertions.

**Tech Stack:** Next.js App Router, TypeScript, Prisma seed, Playwright auth config, Radix Dialog.

---

## Chunk 1: Discover Remaining Modals

**Files:**
- Read: `app/dashboard/admin/subscriptions/page.tsx`
- Read: `app/dashboard/admin/stages/page.tsx`
- Read: `app/dashboard/assistante/coaches/page.tsx`
- Read: `app/dashboard/assistante/credits/page.tsx`
- Read: `app/dashboard/assistante/subscriptions/page.tsx`
- Read: `app/dashboard/coach/sessions/page.tsx`
- Read: related APIs used by these pages

- [x] Identify the exact button/link trigger for each mounted Dialog.
- [x] Identify which triggers require seeded rows.
- [x] Mark any genuinely inatteignable modal with concrete code evidence.

## Chunk 2: Add RED Tests

**Files:**
- Modify: `e2e/auth/dialog-all-roles-proof.spec.ts`

- [x] Add active Playwright tests for each reachable modal, using real auth.
- [x] Use the existing assert battery: visible dialog, role, dark bg, opacity, filet, Fraunces title, focus-trap, AA text contrast, ESC close, focus return.
- [x] Run the targeted spec against current seed and record expected failures.

## Chunk 3: Seed Missing Data

**Files:**
- Modify: `scripts/seed-e2e-db.ts`

- [x] Add only the data needed to render real UI triggers: subscription rows/requests, stage rows, coach/session rows, credit rows as required by discovery.
- [x] Re-run the targeted spec and keep failures only where app charte/a11y is defective.

## Chunk 4: Fix App Defects Exposed by Tests

**Files:**
- Modify only mounted page/component files whose real modal fails the standard assertions.

- [x] Fix controlled-dialog focus return with `onCloseAutoFocus` and trigger refs.
- [x] Fix missing `DialogTitle`/filet/Fraunces by using existing `components/ui/dialog.tsx` primitives.
- [x] Do not change business flows beyond what is required to make the existing UI accessible and provable.

## Chunk 5: Gate, Ship, Report

**Files:**
- Modify: `scripts/gate-all.sh`

- [x] Raise `AUTH_MIN` to the exact new auth total.
- [x] Run `scripts/gate-all.sh` fully and capture `EXIT=0` plus lane totals.
- [ ] Commit and push in the required order.
- [ ] Deploy only if application code changed.
- [ ] Report modal-by-modal proven / inatteignable status honestly.
