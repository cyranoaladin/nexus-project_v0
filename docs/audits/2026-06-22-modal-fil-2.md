# Finition modales fil 2

## Date

2026-06-22

## Contexte

Le fil 1 avait porté la preuve e2e auth à 6 modales prouvées. Le fil 2 étend la preuve aux modales restantes montées et atteignables, avec auth réelle, seed e2e déterministe et assertions charte complètes.

## Problèmes observés

- `admin/subscriptions` n'avait aucun abonnement actif dans le seed e2e.
- `admin/stages` n'avait aucun stage, aucune séance/bilan de stage seedés.
- `assistante/subscriptions` n'avait ni souscription inactive, ni demande `SubscriptionRequest` en attente.
- `coach/sessions` n'affichait pas le bouton Rapport même avec une session confirmée : l'API renvoyait le statut en lowercase alors que l'UI attendait `CONFIRMED`.
- Plusieurs modales contrôlées sans `DialogTrigger` ne restauraient pas le focus au déclencheur après ESC.

## Décisions prises

- Ajouter au seed uniquement les données nécessaires aux triggers réels : abonnements, demande d'abonnement, stage, bilan de stage, session coach du jour.
- Corriger le retour focus à la source avec `onCloseAutoFocus` et refs de déclencheur pour les modales contrôlées.
- Remplacer le wrapper cliquable de `SessionReportDialog` par un vrai `DialogTrigger asChild`.
- Conserver le statut coach en uppercase dans `/api/coach/dashboard` pour aligner API et UI.
- Porter `AUTH_MIN` de 22 à 33.

## Fichiers modifiés

- `app/api/coach/dashboard/route.ts`
- `app/dashboard/admin/stages/page.tsx`
- `app/dashboard/admin/subscriptions/page.tsx`
- `app/dashboard/assistante/coaches/page.tsx`
- `components/ui/session-report-dialog.tsx`
- `e2e/auth/dialog-all-roles-proof.spec.ts`
- `scripts/seed-e2e-db.ts`
- `scripts/gate-all.sh`
- `docs/superpowers/plans/2026-06-22-modal-fil-2.md`

## Tests exécutés

- Seed e2e seul contre `nexus_e2e`.
- RED ciblé `dialog-all-roles-proof.spec.ts`: 9 échecs attendus avant seed/app fixes.
- Run ciblé après seed: 7 échecs focus/API attendus.
- Build local `npx next build`.
- Run ciblé final `dialog-all-roles-proof.spec.ts`: 17 passed.
- Gate complet `scripts/gate-all.sh`: `EXIT=0`.

## Résultats

- 17 modales/preuves dans `dialog-all-roles-proof.spec.ts`.
- Les assertions couvrent ouverture réelle, `role=dialog`, fond sombre, opacity, filet or, titre Fraunces, focus-trap, contraste AA, ESC et retour focus.
- Aucune modale testée ne reste en skip dans cette spec.
- Totaux gate: Jest 6215, public 184, auth 33, total 6432.

## Risques restants

- Les modales nested de rejet dans `assistante/subscriptions` et `credit-requests` ne sont pas ouvertes séparément dans ce fil ; le périmètre demandé des pages restantes est couvert par leurs modales principales.
- Les anciennes modales non montées `credit-purchase` et `invoice-details` restent de la dette fonctionnelle déjà documentée.

## Rollback

- Revenir au commit précédent et remettre `AUTH_MIN=22` si la campagne fil 2 doit être retirée.
