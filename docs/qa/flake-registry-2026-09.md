# Flake registry — CI failures 2026-08-12 → 2026-09-11

Built from the ORIGINAL GitHub Actions job logs (`actions/jobs/{id}/logs`, 139 failure-concluded runs over 14 days, 35 older runs, attempt-1 logs of every re-run run). No event is classified `UNKNOWN`, `PROBABLY_FLAKE` or `COULD_NOT_REPRODUCE`; where a reproduction was not obtained, the root cause is proven from the log plus the code path and that is stated explicitly.

Common CI environment: GitHub-hosted `ubuntu-latest` (ubuntu-24.04), Node 22.23.1, runner 2.337.0. Parallelism: Unit Tests = `jest --ci --coverage --maxWorkers=2`; Integration = `jest.integration.config.js --runInBand`; E2E Parcours Authentifiés = Playwright `workers: 1`, Chromium; ARIA Browser = docker-compose, `workers: 1`. No `--ci` retries, no Playwright `retries`.

## Active events — closed by this PR

| Id | TEST | COMMIT · JOB | TIMESTAMP (UTC) | EXACT_ERROR | REPRODUCED | ROOT_CAUSE_CLASS | FIX |
|---|---|---|---|---|---|---|---|
| A1 | `e2e/auth/core-golden-family.spec.ts` "parent sets a password via the phone-activation link" | main `223285f8` · run 34536217766 att.1 / job 103068326204 (E2E Parcours Authentifiés) | 2026-09-10T22:18:52Z | `expect(page).toHaveURL(/\/auth\/signin\?activated=true/)` received `/dashboard/assistante`; call log: 4× `parent-phone?token=…`, 10× `/dashboard/assistante` | Not reproduced in isolation (timing of a client-side session refresh); root cause proven from the log + code path (below) | **TEST_SHARED_STATE** | `resetBrowserSession(page)` (about:blank before `clearCookies()`) used at the activation step and everywhere else; guard `__tests__/architecture/e2e-browser-session-isolation.test.ts` (RED on the previous e2e tree: 9 offending files; GREEN now) |
| A2 | same test | PR #234 `b77bc149` · run 34632423154 att.1 / job 103372210261 | 2026-09-11T18:24:34Z | identical error and call-log shape | att.2 of the same commit passed (timing) | **TEST_SHARED_STATE** | same as A1 |
| A3 | `e2e/auth/core-golden-family.spec.ts:544` "wrong-course-scope session creation is rejected" | PR #253 `2f22af7a6` · run 34663671736 att.1 / job 103471154144, then PR #249 `c3406e236` · run 34664801051 att.1 / job 103474450242 (both E2E Parcours Authentifiés, both Saturday UTC) | 2026-09-12T01:11:26Z, 2026-09-12T01:34:35Z | `expect(response.status()).toBe(400)` received `409`; body lists the two expected IDENTITY failures (`COURSE_NOT_IN_STUDENT_MAP`, `ASSIGNMENT_COURSE_NOT_IN_SCOPE`) **plus** `STUDENT_CONFLICT` / `COACH_CONFLICT` "déjà une réservation active sur ce créneau" | Reproduced by computation (deterministic, calendar-dependent): the probe slot was `nextWeekdayIso(28)` 10:00 while series B (coach 2 / child B, 10:00, weekly ×3) starts at `nextWeekdayIso(22)`. On a Saturday or Sunday UTC run both roll forward over the weekend onto the same Monday: 2026-09-12 (Sat) → +22 = Sun 10-04 → Mon 10-05 (series B: 10-05, 10-12, 10-19); +28 = Sat 10-10 → Mon 10-12 = 2nd occurrence → the conflict check wins and the API answers 409. Every weekday run (all prior green runs on main) has no collision, which is why it only appeared now | **OTHER_PROVEN** (calendar-dependent test data, not a race) | Probe moved to `seriesBDate` 14:00–14:45 — a slot no series occupies, so the only possible failure is course scope (commit d92bf2c8a, carried by this PR and #253) |
| A4 | `e2e/auth/core-golden-family.spec.ts:168` "parent confirms the household (registrationCompletedAt)" — **webkit-smoke** project | PR #263 `6ae5c800d` · run 34699553053 / job 103568768862 (E2E Parcours Authentifiés, step "cross-browser smoke (Firefox + WebKit)"; the Chromium gate of the same job: 119/119 green) | 2026-09-12T14:45:52Z (job end) | `page.goto: Navigation to "…/dashboard/parent/inscription" is interrupted by another navigation to "…/dashboard"` (raised from `gotoStable`, i.e. both its attempts were interrupted) | Not reproduced: 3/3 WebKit passes of the full scenario on the disposable stack (`playwright.webkit.config.ts`, Desktop Safari device). No trace from CI: the auth lane's `playwright-auth-report` upload was empty at that commit (reporter `line` only — fixed by #235). Mechanism proven from the log + code path: the only code that navigates to the bare `/dashboard` in this flow is `app/auth/signin/page.tsx` (`redirect('/dashboard')` for a live session), and the only client code that navigates to `/auth/signin` from the two documents alive in that step is the `useSession()`-driven effect of `app/dashboard/parent/page.tsx` (`!session \|\| role !== 'PARENT'` → `router.push('/auth/signin')`) and of `app/dashboard/parent/inscription/page.tsx` (`role !== 'PARENT'` → `router.replace('/auth/signin?…')`). The step before asserted only `toHaveURL(/dashboard/parent)` and moved on while the dashboard document's client session resolution was still pending; on WebKit that document's effect then fired its sign-in bounce, which the live session turned into the observed `/dashboard` navigation, racing the next `page.goto` | **READINESS_RACE** (test leaves a document before its authenticated render; WebKit schedules the competing navigation later than Chromium) | "parent logs in by phone" now asserts the rendered authenticated header (`heading 'Espace Famille'`) before leaving the dashboard — the header renders only after `useSession()` resolved to the PARENT session; no retry, no skip (commit carried by #263). Follow-up when a trace exists (#235 restores the artifact): the client-side sign-in bounces in both pages duplicate the middleware guard and can be removed |
| B1 | `__tests__/api/assistante-student-operational-workflow.test.ts` "still links out to assignments" | `535a9603` · run 34601113429 att.1 / job 103268358406 (Unit Tests, `--coverage --maxWorkers=2`) | 2026-09-11T12:57:39Z | `Unable to find role="link" and name "Voir assignations"` — DOM at timeout is the page-level loading spinner | Original test 3/3 green locally under the same recipe (not reproduced: CI runner load); mechanism proven: RTL's default 1 000 ms `findBy` budget vs an instrumented full-page re-render after the mocked fetch | **READINESS_RACE** | explicit load-completion budget (`findByRole(…, { timeout: 10_000 })`) with the contract documented — the assertion is "the loaded page links out", which has no intrinsic 1 s bound; no retry, no skip |
| D1 | `e2e/auth/bilan-golden-path.spec.ts` "passation réelle … → publication" | `e956ea6e` · run 33993323766 att.1 / job 101379412374 | 2026-09-05T21:38:35Z | `expect(drainResult.completed).toBeGreaterThanOrEqual(1)` received **0** (spec:169) | att.2 passed | **TEST_SHARED_STATE** — the disposable stack runs the real in-process scheduler (`docker-compose.e2e.yml: BILAN_WORKER_ENABLED=true`, exercised by `bilan-worker-autonomous.spec.ts` in the same run), so the spec's manual drain is one of two consumers of the same queue; when the scheduler scores first, the manual drain legitimately completes 0 | assert the OUTCOME (score snapshot exists / artifact `PENDING_REVIEW`, `expect.poll` ≤ 30 s) instead of "my drain completed ≥ 1" |
| D2 | same | main `38834958` · run 34092916494 / job 101650050070 | 2026-09-07T07:00:37Z | identical | no rerun | **TEST_SHARED_STATE** | same |
| D3 | same (spec:170 in that revision) | PR #242 `768bb77e` · run 34631157350 att.1 / job 103368035342 | 2026-09-11T18:10:30Z | identical | att.2 passed | **TEST_SHARED_STATE** | same |
| E1 | `e2e/auth/parent-canonical-report-access.spec.ts:178` "le Parent voit uniquement sa restitution publiée" | `3644c735` · run 34389989782 att.1 / job 102595650613 | 2026-09-09T18:45:37Z | `locator.fill: Test timeout of 60000ms exceeded` waiting for the sign-in textbox while a `/dashboard/eleve` navigation was still pending (spec:257) | att.2 passed | **TEST_SHARED_STATE** — the spec's local `signIn()` navigates to `/auth/signin` right after the "Se déconnecter" click without disposing the student document; `/auth/signin` (`await auth()`) bounces the still-live session to `/dashboard/eleve` | `resetBrowserSession(page)` at the start of that `signIn()` |
| F1 | `__tests__/integration/planning-concurrency.real.test.ts` "deux créations concurrentes pour EXACTEMENT le même créneau" | `11d29883` · run 34529418191 att.1 / job 103046198329 (Integration, `--runInBand`) | 2026-09-10T21:00:02Z | `expect(statuses.every(s => s === 201 \|\| s === 409)).toBe(true)` received false; route log: `ConnectorError(… PostgresError { code: "40P01", message: "deadlock detected" …})` | 0/25 local runs reproduced the deadlock (the loser hit the `23P01` exclusion constraint every time); root cause proven from the CI log: the raw `40P01` (and `40001`) shapes are `PrismaClientUnknownRequestError` without `.code`, which `isPlanningConflictDatabaseError` did not recognize → 500 instead of the promised 409 | **PRODUCT_RACE** | `lib/planning/series.ts` predicate now recognizes raw `40001` / `40P01` (message-borne SQLSTATE); regression tests with the exact raw shapes in `__tests__/api/assistante.planning-series.test.ts` (RED 2/2 without the fix, GREEN with it) |

Mechanism behind A1/A2/E1 (one class): Auth.js re-issues the JWT cookie on the client-side `/api/auth/session` refresh. When a spec calls `clearCookies()` while the previous role's dashboard document is still loaded, an in-flight refresh response can land after the clear and resurrect the session; the next `/auth/signin` render (`await auth()`) then redirects to that role's dashboard. Disposing the document first (`about:blank`) makes the clear deterministic. The helper already existed for one path (`signInAs`); the guard now makes it the only path.

## Historical resolved events (log-proven, not flakes)

| Id | Where | EXACT_ERROR | CLASS | Resolution |
|---|---|---|---|---|
| C1 | `assistante-subscription-approval-invariants.spec.ts:18/:50` · `a41d5f09` (branch `integration/parent-whatsapp-sans-credits-20260906`) · 2026-09-06T05:18Z | card shows `Prix 750 TND/mois` without the credits line; **11 other specs failed in the same job** (env/seed) | OTHER_PROVEN (job-level breakage of a WIP branch commit) | branch green on later commits; not present on main |
| C2 | same spec · `327d3f59` · 2026-08-13 | `[E2E] E2E_DISPOSABLE_REDIS_URL is required` | OTHER_PROVEN (missing CI env var, deterministic) | fixed on the `ci/e2e-auth-gate` branch |
| C3 | same spec · `8fe5d139` · 2026-08-13 | `Refusing rate-limit reset against a non-disposable Redis target` | OTHER_PROVEN (CI env misconfiguration, deterministic) | fixed on the same branch |
| G1 | `e2e/aria/practice.spec.ts:53` · `e1f702c5` · 2026-09-11T17:37Z | strict-mode violation: `Retour au cockpit` resolved to 2 elements | INVALID_TEST | fixed by `768bb77e` (scoped locator) |
| H1 | TypeScript Type Check · `7c43d727` (#241) · 2026-09-11T16:07Z | `TS1501` regex `s` flag under es2017 | OTHER_PROVEN (type error) | fixed by `c003ba72` |
| I1 | "Pré-rentrée 2026 documents" typecheck · 12 jobs on #235 and main (latest `9f069349`, 2026-09-11T18:11Z) | `TS2307 Cannot find module '@/core-v2/generated/client'` | OTHER_PROVEN (workflow lacked the Core v2 `prisma generate` step) | fixed by PR #232 (`ab4c42c0`) |
| I2 | "Pré-rentrée 2026 documents" docker build · 24 runs 2026-08-28 → 09-05 | `E: Unable to correct problems, you have held broken packages.` (pinned apt versions) | OTHER_PROVEN (deterministic package drift) | fixed ~2026-09-06 |
| J1 | ARIA Browser (desktop) docker build · `168dffde` · 2026-09-10T10:43Z | `spawnSync …/esbuild ETXTBSY` + `ENOTEMPTY @prisma/engines` during image build, no test ran | RUNNER_INFRA_TRANSIENT_PROVEN (docker-build npm race, log line) | att.2 green |
| K1 | `e2e/auth/admin-aria-preview.spec.ts:27` · `e9f3d018` · 2026-09-04 | strict-mode violation: `aria-preview-root` resolved to 2 elements | INVALID_TEST (deterministic duplicate test id) | branch later green |
| L1 | 12 specs in one job · `1d9017bb` (#235 mid-iteration) · 2026-09-11T08:20Z | e.g. `Expected 200 Received 409`, `getByLabel('Cours ARIA')` timeout | OTHER_PROVEN (branch-local breakage during the spec reshuffle) | #235 final head `9f069349` E2E green |
| M1 | Planning Studio UI specs · `792a49d6`/`0e590587`/`53a6aa73` · 2026-09-04 | viewport-geometry assertions | OTHER_PROVEN (branch-local) | later commits green |
| N1 | dev-iteration bulk on `feat/core-go-live-family-academic-planning-20260906` (10 SHAs, 2026-09-06/07) | same error families across 10 consecutive pushes | OTHER_PROVEN (WIP branch breakage) | landed via other PRs; not on main |
| O1 | `feat/aria-b-conversation-streaming` (Aug 29–31) | `ARIA_TURN_RECOVERY_WORKER_PREFLIGHT_FAILED`, `UnhandledSchemeError: node:crypto`, 1× `ECONNRESET` | OTHER_PROVEN (WIP branch; deterministic) — ARIA lane | branch later green |
| P1 | ARIA Coverage gate · ARIA branches · 2026-09-06 → 09-10 | `ARIA_COVERAGE_GATE_FAILED:…` | OTHER_PROVEN (coverage threshold) — ARIA lane | later green |
| Q1 | main `724f8982` · run 34372366038 (4 attempts) · 2026-09-09 | att.1 ARIA Jest (api): `npm error Error: aborted` during `npm ci` (no test ran) · att.2/3 ARIA Requirement Evidence: `ARIA_VISUAL_EVIDENCE_INVALID:ARTIFACT_ROOT_MISSING` (re-running only the failed jobs left the evidence job without its upstream artifact) | att.1 RUNNER_INFRA_TRANSIENT_PROVEN · att.2/3 OTHER_PROVEN (workflow shape) — **ARIA lane, handoff**: the evidence job should fail-fast with a clear "rerun the full workflow" message or re-download its producer's artifact | att.4 (full rerun) green |

## Section J — open handles

- 30 days of CI logs: no Jest open-handle warning. Only leak-class signal: `MaxListenersExceededWarning: 11 SIGTERM/SIGINT listeners added to [process]` (Unit Tests jobs 99369030377, 99271018645, 99258320401) → **ASYNC_RESOURCE_LEAK**, root cause and fix in PR #246 (globalThis-guarded `process.once` registrations re-added per Jest file; registry moved onto `process`; regression test RED 4/4 → GREEN 4/4).
- `--detectOpenHandles` on current main with the CI recipes: Unit 1165 suites / 13263 tests, Integration 55 + 1 + 1 suites / 301 + 3 + 10 tests — **0 handles reported**, exit 0.
- The originally reported local "leaked timer" event is absent from CI logs and not reproducible: ORIGINAL_EVENT_PROVEN_EXTERNAL/STALE.

## Open observations (not test failures — owner items)

1. `BILAN_PARENT_PUBLISH_NOTIFICATION_FAILED <revisionId> Error` is logged in the E2E app during D1–D3 (and other runs). The log deliberately prints only the error **name** (`Error`), so the cause is not classifiable from logs; the notification is documented as comfort-only and the spec does not assert it. Recommendation: log the error code (never PII) so a future event can be classified.
2. Q1 attempts 2–3: ARIA-owned workflow shape (evidence job after a partial rerun) — handoff to the ARIA owner.

Final state after that PR and #246: ACTIVE_FLAKE_EVENTS = 0, FLAKY_TESTS = 0, UNKNOWN = 0, OPEN_HANDLE_WARNINGS = 0.

*That line records the state as of 2026-09-11. Nine further events occurred on 2026-09-14/15 and are recorded below; the current totals are at the end of this file.*

## Cycle 2026-09-14 → 2026-09-15

Nine events. Six closed, **R9 open**. Four share one mechanism with A1/A2/E1 above — an in-flight `/api/auth/session`-class request crossing a lifecycle boundary with nothing waiting for it — which is now five occurrences of that single class in this file.

| Id | TEST | COMMIT · JOB | TIMESTAMP (UTC) | EXACT_ERROR | REPRODUCED | ROOT_CAUSE_CLASS | FIX |
|---|---|---|---|---|---|---|---|
| R1 | `e2e/auth/auth-client-lifecycle.spec.ts` "the real session provider still observes revocation on focus after recovering from a network failure" | originally filed against `main` in PR #277 | 2026-09-13/14 | intermittent: revocation not observed on focus; the boundary stays AUTHENTICATED | Reproduced deterministically in-process, not on CI: `__tests__/auth/controlled-session-route-barrier.test.ts` drives a faithful legacy harness (mutable `Route[]`, non-atomic `Promise.all` over a snapshot) with deferred promises and observes the drain returning while a request admitted after the snapshot is still unsettled | **TEST_HARNESS_REQUEST_DRAIN_RACE** — NOT `RUNNER_INFRA_TRANSIENT_PROVEN` as #277 recorded. Two windows: accounting covered only *intercepted* requests, so one issued after `unroute` was never counted; and `detach()` removed the `requestfinished`/`requestfailed` listeners **before** `unroute`, leaving a window where a matching request was intercepted, continued and unaccounted. `drain()` then returned, `assertIdle()` passed vacuously, and revocation began with a pre-revoke response in flight, which repopulated `__NEXTAUTH._session` | #280 narrowed it; #281 closed it. `closeAndDrain()` is the single shutdown authority (OPEN→CLOSING→CLOSED); accounting is driven by the page's own `request` lifecycle so every matching request is counted whether intercepted or not; `activeHandlers` is tracked; interception is torn down **before** the accounting listeners, with a second quiescence check in between. Closure `a454293d0`. Mutation-checked: 18/19 of the new assertions fail against the pre-fix barrier, the teardown-ordering ones behaviourally |
| R2 | `e2e/auth/auth-client-lifecycle.spec.ts` "ten seconds of unavailable verification preserve the modal draft through late server response without replay" | PR #281 `7df89236` · job 104174313601 (mobile-smoke); PR #282 `6d608128` (mobile-smoke); PR #282 `fc96ef87` (chromium) | 2026-09-14T22:28Z, 2026-09-15T08:06Z, 2026-09-15T08:17Z | `expect(locator).toHaveAttribute` failed — Expected `"UNAVAILABLE"`, Received `"AUTHENTICATED"`, 19 polls over 15 000 ms | Root cause proven from the CI trace: six session requests completed before the refresh and the provoked one shows `time: -1`, i.e. it was **held**, not aborted. A held first refresh leaves the boundary AUTHENTICATED until a client timeout that outlasts the assertion | **TEST_READINESS_RACE** — `requestProviderRefresh` only posts a `BroadcastChannel` message and returns, so the request it provokes arrives *after* the `firstRefresh` flag the test clears on the next line. Whether the first refresh was aborted (fast UNAVAILABLE) or merely held was a race the test had always been running and happening to win. **Pre-existing on `main`, not introduced by #281**: #282 contains none of the barrier code and failed identically | #281: the intent ("the first intercepted refresh fails outright") is expressed as `record.id === 1`, assigned by the barrier in arrival order — the same determinism the sibling test at line 228 already had from `abortCount: 1`. The flag is gone. Closure `a454293d0` |
| R3 | `__tests__/components/ui/session-booking.test.tsx` "auto-selects the sole active assignment and course, then books through the new canonical contract" | PR #281 `b7eae1a2`, PR #282 `6d608128` and `fc96ef87` · Unit Tests; also 3/3 locally | 2026-09-15T08:00Z onward | `Unable to find an element by: [data-testid="booking-title"]` | Reproduced deterministically, locally and in CI, on every branch including ones touching nothing near it — and under `TZ=UTC`, so not a timezone effect | **NOT A FLAKE — EXPIRED TEST DATA.** The test pinned its available slot to `2026-09-14`. `components/ui/session-booking.tsx:519` disables a slot whose date is before today (`isDateInPast`), read from the real clock. Once that date became the past the slot button was disabled, the click was inert, `selectedSlot` stayed null, step 2 never advanced and step 3 never rendered. It passed on `main`'s run of 2026-09-14 **because that run was dated 2026-09-14**, and was destined to fail the next day | #281: the slot is derived from the clock (tomorrow — inside the window the component accepts, far short of the three-month `isDateTooFar` bound), and the expected `scheduledDate` is derived the way the component derives it (UTC-midnight parse, local-time format) rather than assuming they agree. Closure `a454293d0` |
| R4 | `e2e/auth/parcours-eleve-stmg-survival.spec.ts` "affiche les zones tactiques et persiste une copie de phrase" | `main` `e9a6ff9e` · job 104365202887 (E2E Auth — Chromium) | 2026-09-15T11:49Z | `expect(locator).toBeVisible()` failed on `getByText(/Copiée [1-9][0-9]* fois/i)` — **at the assertion AFTER `page.reload()`**, not the one before | Root cause proven from the code path: `PhraseMagiqueCard` increments optimistically *before* the POST and discards its result (`.catch(() => undefined)`), so the pre-reload assertion never proved persistence; `click()` resolves once the handler is dispatched, not when its awaited fetch settles | **TEST_READINESS_RACE** (same shape as R2/R5: an in-flight request crossing a lifecycle boundary). `page.reload()` could abort the POST in flight; the reloaded page then read the server's count, still 0 | #283: wait for the POST to settle before reloading and assert it returned 200. This also strengthens the test — the route rate-limits one copy per user and phrase per second and answers 429, which the client hides, so a throttled write previously looked identical to a successful one. Closure `1c2bcb8a0` |
| R5 | `e2e/auth/core-golden-family.spec.ts:81` "golden family: full lifecycle…" at step "parent dashboard shows child A and child B independently, never merged" | `main` `e9a6ff9e` · E2E Auth — Cross-browser (**firefox-smoke**) | 2026-09-15T12:0xZ | `locator.fill: Test timeout of 60000ms exceeded` waiting for `getByRole('textbox', { name: 'Téléphone WhatsApp ou email' })`, raised from `signInAs` (`e2e/helpers/golden-family.ts:191`) | Root cause proven from the code path: the textbox never appeared because the page was never on the sign-in form — `/auth/signin` renders through `await auth()` and redirects to the role's dashboard when a session is live | **TEST_SHARED_STATE — same class as A1/A2/E1.** Auth.js re-issues the JWT cookie on the client-side `/api/auth/session` refresh, so a refresh still in flight can land its `Set-Cookie` **after** `clearCookies()`. `resetBrowserSession` already disposed the document with `about:blank` — which is why the A1/A2/E1 events stopped — but `about:blank` only stops NEW refreshes being issued; it cannot recall one already on the wire. The window was narrowed, not closed, and Firefox lost that race | #283: the clear is now verified rather than assumed — after clearing, the helper asks the server what it still resolves through the same cookie jar. That request is issued *after* the clear, so a resolved session can only mean a straggler landed in between, and clearing again converges because no document remains to issue another. Bounded at five attempts, then it throws naming what the server still sees. 27 call sites inherit it, including `loginAsUser`. Closure `1c2bcb8a0` |
| R6 | `e2e/auth/core-golden-family.spec.ts:81` "golden family: full lifecycle…" at step "parent dashboard shows child A and child B independently, never merged" — **firefox-smoke** | `main` post-merge run of #273 · run 35013424258 / job 104530775420 (E2E Auth — Cross-browser) | 2026-09-15T19:33:19Z | `locator.fill: Test timeout of 60000ms exceeded` waiting for `getByRole('textbox', { name: 'Téléphone WhatsApp ou email' })`, raised from `signInAs` (`e2e/helpers/golden-family.ts:200`) | Not reproduced in isolation; root cause proven from the CI trace captured on #285, which timed the whole window at 34 ms: `19:32:26.819` `200 /api/auth/session` (server: no session) → `19:32:26.853` `307 /auth/signin` (server: you are admin) → `19:32:26.875` `200 /dashboard` | **TEST_SHARED_STATE — R5 RECURRENCE, same class as A1/A2/E1/R5.** R5's fix verified the clear by asking the server what it still resolved through the same cookie jar. But that probe and the subsequent navigation cannot be atomic: an Auth.js refresh already on the wire can re-issue the cookie *between* the verified clear and `/auth/signin`. Every fix through #283 tightened a **pre-check**, and this window is not pre-checkable in principle | #285: `gotoSignInForm` stops pre-checking and observes the **outcome** — if the navigation did not land on the form, the session was resurrected, so clear and retry, bounded at three attempts, then throw naming where it was sent. This PR converges the two remaining call sites that still navigated raw after clearing (`parent-canonical-report-access.spec.ts`, `session-revocation.spec.ts`) and adds `__tests__/architecture/signin-navigation-authority.test.ts` so a clear-then-raw-navigate cannot reappear. Mutation-checked: 7/7 mutations (3 call sites, 4 on the helper body) turn the guard red |
| R7 | `__tests__/integration/npc-submission-lock.real.test.ts` "NPC common submission lock on PostgreSQL 15 › freezes inventory when queueing wins against delete one of multiple copies" | `main` post-merge run of #287 · run 35082406437 / job 104749266027 (Integration Tests, step "Run NPC real database integration tests"; the main integration lane of the same job: 56 suites / 305 tests green) | 2026-09-16T10:01:49Z | `SubmissionInventoryFrozenError: Submission document inventory is frozen`, raised from `assertSubmissionInventoryMutable` (`lib/npc/submission-inventory.ts:30`) via the test's own competing transaction — **the error the test asserts**, surfacing as a failure instead | Not reproduced in isolation (3/3 green locally before the fix, on the real `scripts/testing/run-npc-real-db-tests.sh` harness); root cause proven from the statement ordering, not from timing | **TEST_DEFERRED_REJECTION_WINDOW — same family as A1/E1/R2/R4/R5** (an in-flight promise crossing a boundary with nothing waiting for it). `const mutation = secondClient.$transaction(...)` is blocked on the row lock; `releaseQueue.resolve()` frees it; the competing transaction then acquires the lock, throws and settles **during `await queue`** — one line before `await expect(mutation).rejects` attaches a handler. Node reports an unhandled rejection and fails the run. The log shows `PromiseRejectionHandledWarning: Promise rejection was handled asynchronously (rejection id: 67)` immediately before the failure, which is exactly that window. **Not caused by #287**, which touched only a guard test, a doc and two e2e specs; Integration Tests was green on the five preceding `main` runs | #288: both race blocks in that file capture the outcome at creation (the sibling tombstone block had the identical shape and had simply not lost the race yet). Nine further repo-wide sites take the minimal remedy `void p.catch(() => {})` immediately after creation, which marks the promise handled without touching any assertion — an idiom `__tests__/auth/session-recovery-controller.test.ts` already used. `__tests__/architecture/deferred-rejection-assertion-authority.test.ts` keeps the class closed, flagging only an `expect(<name>).rejects` whose promise was created in the same block with an `await` in between |
| R8 | `e2e/auth/auth-client-lifecycle.spec.ts:153` "the real session provider still observes revocation on focus after recovering from a network failure" (**the test R1 tracks**) — chromium | PR #288 `b9bac29e5` · run 35085816106 / job 104760248968 (E2E Auth — Chromium) | 2026-09-16T10:42:50Z | `ControlledSessionRouteBarrier closeAndDrain(post-unroute) timeout after 15000ms. state=CLOSING, started=4, held=0, released=2, finished=2, failed=1, activeHandlers=0, outstanding=1` | Reproduced deterministically in-process before fixing: `__tests__/auth/controlled-session-route-barrier.test.ts` "a request held across a navigation cannot hang the drain once released" (red, then green) | **TEST_HARNESS_REQUEST_DRAIN_RACE — #286 closed only half of it.** #286 made a main-frame navigation settle every request the browser cancels, but deliberately skipped records a route handler still owned, because settling one behind its owner would be wrong (a test asserts that invariant). The complement stayed open: when the navigation lands **while** a request is HELD, its document dies; the handler then releases the request normally and nothing ever reports it, because the browser emits neither `requestfinished` nor `requestfailed` for a request whose document no longer exists. The record sits in `RELEASED` and the drain waits on it forever. The counters say exactly this — started 4, accounted 3, one outstanding that is neither held nor owned by a handler | #288: the navigation marks such a record `doomedByNavigation` instead of skipping it silently, and the release path settles it as `CANCELLED_BY_NAVIGATION`. The held request is still never stolen from its handler — that test is unchanged and still passes. Barrier suite 21 → 22 |
| R9 | `e2e/auth/auth-client-lifecycle.spec.ts:153` "the real session provider still observes revocation on focus after recovering from a network failure" (**R1's test, third distinct mechanism in one day**) — chromium | PR #247 `0f02072be` · run 35095526042 / job 104791788822 (E2E Auth — Chromium) | 2026-09-16T12:31:12Z | `expect(locator).toHaveAttribute` failed at spec:192 — Expected `"AUTHENTICATED"`, Received `"RECOVERING"`, 9 polls over 5000 ms | Trace obtained and read (`playwright-auth-chromium-report`, restored by #235). **Mechanism NOT fully established — deliberately recorded as open.** | **UNKNOWN_PROVIDER_RECOVERY** — what the trace proves: the induced failure occurred as designed (`net::ERR_FAILED`, Auth.js `J: Failed to fetch` from `_getSession`); **seven subsequent `/api/auth/session` responses returned 200**, plus one entry at `time: -1` (held, not aborted — the distinction R2 established); yet the boundary never left `RECOVERING`, and the page snapshot shows the recovery UI with its manual "Réessayer la vérification" button still offered. Why seven successful reads did not return the provider to AUTHENTICATED is **not** proven. **Attribution is proven negative for #247**: it touches no auth machinery (`middleware.ts`, `auth.ts`, `auth.config.ts`, `components/auth/`, `lib/auth/` all untouched); its only shared-code change is an additive `core-v2-activation` entry in `lib/rate-limit/sensitive.ts`, and rate-limit buckets are keyed by policy name (`${scope}:ip`), so it cannot share a bucket with anything serving `/api/auth/session` | **No fix attempted.** Three mechanisms in one day on this one test (drain timeout pre-#286; held-across-navigation, fixed in #288; this). #281 and #286 each looked complete on a green run and each left a reachable path open. A fourth blind patch is not warranted: the missing instrument is the repeat campaign R1 has been waiting for, still blocked because `ci.yml` has no `workflow_dispatch` trigger |

### Corrections to earlier records

- **#277's diagnosis is superseded.** It recorded R1 as `RUNNER_INFRA_TRANSIENT_PROVEN`. The real cause is `TEST_HARNESS_REQUEST_DRAIN_RACE`, proven by a deterministic in-process reproducer and by mutation-checking the fix. #277 should be closed as superseded rather than merged; its `docs/qa` delta is replaced by the R1 row above.
- **R1's first "reproducer" proved nothing.** The suite added with #280 asserted `const firstResponseSettled = true` and `const requestBInFlight = true` — hardcoded constants that cannot fail — and synchronised with `setTimeout(20)`. It is replaced by the legacy-harness reproducer described in the R1 row. No test in the replacement sleeps.
- **A1/A2/E1 are not fully closed by `about:blank` alone.** See R5: that fix narrowed the window rather than closing it.
- **R5's recorded closure was itself premature — corrected by R6.** R5's row states the class "is closed now that the clear is verified" (closure `1c2bcb8a0`). It was not: the same test failed with the same signature on the post-merge `main` run of #273 (run 35013424258, 2026-09-15T19:33:19Z), *after* that closure landed. Verifying the clear removed one more way to lose the race but left the decisive one, because the verification and the navigation cannot be made atomic. Four successive fixes (A1/A2/E1 `about:blank`, then #283's verified clear) all tightened a pre-check for a window that cannot be pre-checked. The class is closed by **observing the outcome** instead (#285's `gotoSignInForm`), by converging the last two call sites, and by the guard that keeps the raw pattern from returning. *Rule this file should apply to itself: a closure claimed from a green run is a hypothesis; only a fix whose mechanism excludes the failure mode is a closure.*

**Correction this event forces on R1's status.** R1 was recorded `FIX_LANDED, QUALIFICATION_PENDING` after #281, and #286 was described at the time as "correct by construction and consistent with observation" but explicitly **not proven**, because one green run is evidence rather than proof for an intermittent defect. R8 is the counter-evidence, and it arrived from ordinary CI repetition rather than from a qualification campaign — which is precisely the argument for the repeat harness that remains blocked on `ci.yml` having no `workflow_dispatch` trigger. Two successive fixes to this barrier each looked complete and each left a reachable path open; the class should not be called closed on a green run again.

### Current totals (2026-09-16, `main` = `9a65ffb9a`)

ACTIVE_FLAKE_EVENTS = 1 (R9, mechanism not established) · FLAKY_TESTS = 1 (`auth-client-lifecycle.spec.ts:153` — three distinct mechanisms on 2026-09-16 alone: R8, R9, and the pre-#286 drain timeout) · UNKNOWN = 0 · OPEN_HANDLE_WARNINGS = 0 · EXPIRED_TEST_DATA = 0 (R3 closed)

R1 remains **FIX_LANDED, QUALIFICATION_PENDING** — but the reason has changed. The blocker was that the repeat campaign (WebKit 30/30, Firefox 20/20, Chromium 20/20, retry=0) could not be executed at all: no harness existed. **`.github/workflows/flake-qualification.yml` now provides it**, on `workflow_dispatch`, deliberately as a standalone workflow rather than an edit to the required `ci.yml` gate. The container digests it needs were already pinned in `.github/governance/container-images.json` by #282, so no allowlist work remains. R1 stays PENDING only until the campaign is actually run.

**Why this harness stopped being optional.** `auth-client-lifecycle.spec.ts:153` failed three times on 2026-09-16 with three different mechanisms, and both #281 and #286 were declared complete on the strength of a single green run and each left a reachable path open. One green run cannot distinguish a closed race from a race that did not fire. Until a fix is qualified by repetition, every closure on this file is a hypothesis.

## Cycle 2026-09-16 → 2026-09-17

Five events. Two of them I caused myself while fixing the other three, and both by the same mistake: asserting a value I had not measured. The qualification campaign R1 had been waiting for was finally executed, and its result is negative in the useful sense.

| Id | TEST | COMMIT · JOB | TIMESTAMP (UTC) | EXACT_ERROR | REPRODUCED | ROOT_CAUSE_CLASS | FIX |
|---|---|---|---|---|---|---|---|
| R10 | `e2e/public/navbar-mobile-a11y.spec.ts:8` "mobile menu a11y: role, aria-modal, ESC, focus-return" | `main` `c99cc5de9` · run 35104958189 / job 104823653075 (E2E Tests) | 2026-09-16T14:08:47Z | `expect(closeBtn).toBeFocused({ timeout: 2000 })` failed at spec:27 — 1 failed / 379 passed | **Reproduced and measured**, not inferred: a standalone HTML probe reproducing only the CSS under test, driven by a throwaway Playwright config inside the repo tree. `immediate focus() in the effect → NOT_FOCUSED`, `double requestAnimationFrame → FOCUSED`, `bounded task-queue retry → FOCUSED` | **BROWSER_FOCUSABILITY_TIMING.** The close button sits in a panel whose `visibility` is driven by a transition. `focus()` on a not-yet-focusable element fails **silently** — no exception, no return value, nothing to notice — so the double `requestAnimationFrame` the code had was not decoration | #291 (merged 2026-09-16T19:46): focus driven by the outcome rather than by a frame count, bounded task-queue retry, no `requestAnimationFrame` anywhere in the focus path, `clearTimeout(focusTimer)` in cleanup |
| R10-bis | same spec, **plus** `navbar-mobile-a11y.spec.ts:43` "mobile menu focus trap wraps on Tab" | PR #291 `be3bb56ad` · run 35110014640 / job 104840973868 | 2026-09-16T14:55:35Z | both `toBeFocused` assertions failed (spec:27 and spec:53) — **2 failed** / 378 passed | Deterministic | **SELF-INFLICTED — REASONING SUBSTITUTED FOR MEASUREMENT.** My first version of #291 replaced the double `requestAnimationFrame` with an immediate `focus()`, reasoned from the CSS Transitions spec: a `visibility` change *towards* `visible` applies immediately, therefore the element must already be focusable. It is not. The "fix" turned one intermittent failure into two deterministic ones | Replaced before merge by the version above. The owner had already approved the wrong version; they were told plainly not to merge it. *The probe cost two minutes and refuted the reasoning in the same run* |
| R11 | `e2e/auth/auth-client-lifecycle.spec.ts:153` "the real session provider still observes revocation on focus after recovering from a network failure" (**R1's test**) — **mobile-smoke**, then **chromium** | PR #291 `a95e43cd8` · run 35133883927 / job 104921286188 (E2E Auth — Cross-browser) · then PR #255 `8a97ae794` · run 35189824986 / job 105099838860 (E2E Auth — Chromium) | 2026-09-16T18:32:51Z · 2026-09-17T06:32:57Z | **byte-identical on both**: `ControlledSessionRouteBarrier closeAndDrain(post-unroute) timeout after 15000ms. state=CLOSING, started=4, held=0, released=2, finished=2, failed=1, activeHandlers=0, outstanding=1` — the same signature R8 records | Not reproduced in isolation; see R12 — 980 isolated executions of this spec produced zero failures | **TEST_HARNESS_REQUEST_DRAIN_RACE — R8 RECURRENCE. #288 did not close it.** One record is started, never intercepted, and nothing the page emits will ever report it, so no amount of waiting can account for it. #286 settled requests the browser cancels by navigation; #288 settled a request held across a navigation once released. Neither covers a request the harness never owned and the page never finishes | #297 (merged 2026-09-17T10:41): `_accountUnreportableAtDeadline()`. **At the drain deadline only** — never before it — any record still unaccounted and never intercepted is closed as `UNREPORTABLE_AT_SHUTDOWN`; if that leaves the barrier drained, the drain resolves instead of rejecting. Deadline-only, so it cannot mask a request that could still have been reported. **Status: FIX_LANDED, NOT QUALIFIED** — this is the fifth closure attempt on this barrier and the file's own rule applies |
| R12 | R1's qualification campaign, executed at last | chromium ×20: run 35147158715 (`ba7f19171`) · firefox ×20: run 35149929444 (`ba7f19171`) · webkit ×30: run 35170160591 (`a2d79577f`) | 2026-09-16T20:32Z · 2026-09-16T21:00Z · 2026-09-17T01:20Z | — | **280 passed (18.8m) · 280 passed (19.2m) · 420 passed (33.8m) = 980 executions, `retries: 0`, zero failures** | **NEGATIVE RESULT, AND IT IS THE USEFUL ONE.** Repetition of this spec *in isolation* does not reproduce the defect. R11's two occurrences then arrived from ordinary full-pipeline runs, on two different projects. The trigger is contention in the full pipeline, not repetition of the spec — which is exactly why four successive barrier fixes each looked complete on one green run | No fix; the campaign is an instrument. `flake-qualification.yml` needed three follow-ups to finish its own documentation step: #290 (an `env` block for the `${{ env.X }}` references, plus `__tests__/architecture/workflow-env-reference-authority.test.ts` so an undefined one cannot reappear), #293 (job `timeout-minutes: 60`), #295 (step `timeout-minutes: 50`) |
| R13 | `e2e/auth/core-v2-staff-golden.spec.ts:51` "golden staff workflow on Core v2…" — **firefox-smoke and webkit-smoke** | PR #251 `8754d4b60` · run 35144875422 / job 104958951033 (E2E Auth — Cross-browser) | 2026-09-16T20:19:49Z and 20:22:00Z | at "a CURRENT academic year exists (staff API)": `{"ok":false,"error":{"code":"CORE_V2_UNAVAILABLE","message":"Core v2 is not configured on this deployment."}}` — `Expected: 201`, `Received: 503`. **2 failed** / 63 passed | Deterministic | **NOT A FLAKE — CI_LANE_ENVIRONMENT_GAP.** The cross-browser job had no Core v2 service, no `prisma migrate deploy` against it and no `CORE_V2_*` environment. The spec had only ever run in the chromium job, which has them. A lane was executing a spec it could not satisfy | #251: Core v2 service + migration + environment added to the cross-browser job, `CORE_V2_AUTH_MODE=HYBRID`, matching the chromium job |
| R13-bis | same spec, **webkit-smoke only** | PR #251 `6757802ef` · run 35149514048 / job 104973954392 | 2026-09-16T21:08:29Z | `page.waitForURL: Test timeout of 60000ms exceeded`, raised around spec:79 (`getByRole('heading', { name: /Foyer Amel Corev2/ })`). 1 failed / 64 passed | Not reproduced in isolation | **TEST_SHARED_STATE — SHARED FIXTURE ACTOR.** The cross-browser job runs firefox **then** webkit against the *same* server and the *same* database. Any fixture datum that does not derive from the per-run nonce therefore accumulates across projects: the spec used a hardcoded parent phone number, and asserted on a seeded coach's card that both projects had by then written to | #251: the phone is derived from the nonce (`+216 2` + `nonce % 10_000_000`, zero-padded), and the coach assertions are scoped to the list item carrying the nonce (`getByRole('listitem').filter({ hasText: … })` + `toHaveCount(1)`) instead of to the card that contains every assignment. **Rule this generalises:** in a job that runs several projects against one stack, every fixture datum must derive from the nonce and every assertion must be scoped to the entity that carries it — never to a card-level container |
| R14 | `e2e/aria/production-smoke.spec.ts` "ARIA-B … academic capability overlay" | PR #296 `78a791861` · run 35214268861 / job 105178883056 (ARIA Browser (smoke)) | 2026-09-17T11:2xZ | `expect(courseKeys).toEqual(academicCourseKeys)` — `Received: []` against the keys the API had already returned | Not reproduced in isolation | **TEST_READINESS_RACE** — same family as R2/R4/R5. `evaluateAll` is a one-shot read and `expect(array).toEqual` does not retry, so reading straight after the panel-opening click races the panel populating its `<select>`. **Attribution proven negative for #296**, which touches only `.gitignore`, `scripts/security/check-assessment-confidentiality.mjs` and that script's test | #298: a retrying `toHaveCount` before the read, which makes the read deterministic |
| R14-bis | same test | PR #298 `93ae2ae3b` · run 35217869474 / job 105190552544 | 2026-09-17T12:24:57Z | `toHaveCount` — `Expected: 10`, `Received: 11`, **stable across 14 polls** ("14 × locator resolved to 11 elements") | Deterministic | **SELF-INFLICTED — SAME MISTAKE AS R10-bis, ON A COUNT INSTEAD OF A FOCUS.** My first version counted every `option`. `AriaChatPanel` renders, conditionally, up to one `value=""` placeholder ("Aucun cours disponible" / "Choisir un cours") alongside one option per course — which is exactly why the original code carried `.filter(Boolean)`. I asserted on a number the component never promises | #298, corrected: the count is taken on `option[value]:not([value=""])`, after which `.filter(Boolean)` is dead weight and goes. **Rule:** before asserting a count, read the component that renders the collection and enumerate what it can emit. A count assertion is a claim about the component's contract, not about the data you happen to have |

### Corrections this cycle forces on earlier records

- **The campaign is no longer blocked.** The previous totals block states that R1 "stays PENDING only until the campaign is actually run", the harness being available but unused. It has been run — 980 executions, zero failures (R12). The blocker described there is closed; what replaces it is stated in R12.
- **R8's closure was premature, in the same way R5's was.** #288's row says it closes the held-across-navigation window, and it does. It does not close R8's *error signature*, which recurred byte-identically twice after #288 merged (R11). The file's own rule — *a closure claimed from a green run is a hypothesis; only a fix whose mechanism excludes the failure mode is a closure* — now applies for the fifth time on this one barrier.
- **A second failure mode belongs in this file, and it is mine.** Twice this cycle (R10-bis, R14-bis) a fix of mine turned an intermittent failure into a deterministic one, both times because I asserted a value I had reasoned about instead of measuring. Neither is a flake; both cost a full pipeline. Recorded here because the registry's purpose is the failure record, not a record of failures with a convenient author.

### Current totals (2026-09-17, `main` = `0a0e75b84`)

ACTIVE_FLAKE_EVENTS = 2 (R9, mechanism not established; R11, fix landed but not qualified) · FLAKY_TESTS = 1 (`auth-client-lifecycle.spec.ts:153` — now **five** distinct mechanisms) · UNKNOWN = 0 · OPEN_HANDLE_WARNINGS = 0 · EXPIRED_TEST_DATA = 0 · SELF_INFLICTED_DETERMINISTIC_FAILURES = 2 (R10-bis, R14-bis)

R1 moves from **FIX_LANDED, QUALIFICATION_PENDING** to **QUALIFIED IN ISOLATION, NOT QUALIFIED UNDER CONTENTION**. 980 isolated executions found nothing; two full-pipeline runs found the same signature twice. The instrument that is still missing is repetition *of the whole pipeline*, not of the spec.
