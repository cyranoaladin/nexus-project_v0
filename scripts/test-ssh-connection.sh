#!/usr/bin/env bash
set -euo pipefail

echo "Production SSH diagnostics are intentionally absent from the public repository." >&2
echo "Run connectivity checks only from the owner-controlled operations workspace." >&2
exit 1
