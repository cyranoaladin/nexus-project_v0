# Dependencies
node_modules/

# Python virtualenv
.venv/
venv/

# Logs
*.log
.logs/

# Env files (keep examples)
.env
.env.local
.env.*
!.env.example
!.env.local.example
!.env.e2e.example
!.env.ci.example

# OS
.DS_Store

# Storage / reports
storage/
data/invoices/

# Screenshots (runtime artifacts)
screenshots/

# Backup files
*.bak

# Test artifacts
test-results/
playwright-report/

# Build/audit/trace artifacts (never commit)
tmp/
e2e-results/
audit-captures*/
*.lhr*.json
*.trace.zip
e2e/.auth/*.json
# Archives
*.tgz

# Local audit artifacts
audit-public-front-*/
*.local-audit.html
nexus_prod_tree_*.txt
nexus-codex-handoff/
files (*).zip

# Build artifacts
.next/
out/
build/
dist/
coverage/
.nyc_output/
tsconfig.tsbuildinfo

# Misc
.next-*.pid
*.tgz
playwright-report/
test-results/

# Prisma (Database artifacts)
prisma/*.db
prisma/*.db-*
prisma/**/*.db
prisma/**/*.db-*
prisma/.next/
prisma/**/node_modules/

# Database backups
*.sql
*.sql.gz
backup_*.sql
backup_*.sql.gz
# But keep Prisma migration SQL files
!prisma/migrations/**/*.sql
n# Incident / forensic dumps with real DB data
incident-forensics/
e2e/.credentials.json

# ─── Sécurité / RGPD ───────────────────────────────────────────────────────
# Fichiers de session Playwright + fixtures (contiennent des tokens/credentials de test)
student.json
parent.json
admin.json
coach.json
assistante.json
*.auth.json
e2e/fixtures/*.json

# Données stages locales JSON (source de vérité : DB Prisma)
data/stages/*.json
scripts/data/*.real.json
scripts/data/*students*.json

# ─── PII / Données nominatives ────────────────────────────────────────────
diagnostic_*.json
!diagnostic.example.json
diagnostic_maths_*.json
prisma/seed-zakaria*
prisma/seed-demo-student.ts
storage/documents/
docs/attestations/
docs/audits/*attestation-travail*.md
*.csv
!examples/**/*.csv
!fixtures/**/*.csv

# ─── Scratch / Temp ──────────────────────────────────────────────────────
scratch/

# ─── Artefacts d'analyse serveur ─────────────────────────────────────────
prod-tree*.txt
nexus-prod-tree-*.txt
nexus-server-*.txt
get-users-temp.*

# ─── Éditeurs IA / Outils (personnel, pas projet) ───────────────────────
.aider*
.codex
.claude/
.superpowers/
# Note: .cline/, .clinerules/, .windsurf/ sont versionnés volontairement
# (skills et workflows métier du projet)

# ─── Dumps / Backups locaux ──────────────────────────────────────────────
*.dump
backups/

# ─── Logs runtime ────────────────────────────────────────────────────────
.pm2/

# ─── Certificats SSL (ne jamais committer) ───────────────────────────────
nginx/ssl/*
!nginx/ssl/.gitkeep
arborescence*.txt

# ─── NSI exam subjects (large PDFs, not versioned) ───────────────────────
Sujets-EP-NSI-2026/

# ─── PNG source files for landing WebP images (large, not used at runtime)
public/images/accompagnement-personnalise.png
public/images/bilans-livrets-personnalises.png
public/images/cta-final-whatsapp.png
public/images/finish-8-juin.png
public/images/forfaits-formules.png
public/images/hero-study-session.png
public/images/nexus-select.png
public/images/premiere-finish-pack.png

eam_prep_nexus.html

# Regenerated audit artefacts (npm run audit:site-map)
docs/architecture/SITE_MAP.md
docs/architecture/SITE_GRAPH.mmd
docs/architecture/SSOT_MAP.md
build-output.log
