name: Matrix producer
on: [pull_request]
jobs:
  aria-jest:
    name: ARIA Jest (${{ matrix.lane }})
    strategy:
      matrix:
        lane: [unit, api]
    runs-on: ubuntu-latest
    steps:
      - run: echo ok
