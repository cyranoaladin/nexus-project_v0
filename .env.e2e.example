# E2E environment (example)
# Copy to .env.e2e locally and fill with safe values. Do NOT commit real secrets.

E2E_BASE_URL=http://localhost:3003
NEXTAUTH_SECRET=please_change_me_min_32_chars

# Use direct OpenAI in dev only if you set a real API key locally
DIRECT_OPENAI_DEV=1
OPENAI_API_KEY={{OPENAI_API_KEY}}
OPENAI_MODEL=gpt-4o-mini

# Embeddings (OpenAI)
OPENAI_EMBEDDINGS_MODEL=text-embedding-3-large
VECTOR_DIM=3072

# Database (dev default per docker-compose.dev.yml)
DATABASE_URL=postgresql://postgres:postgres@localhost:5433/nexus_dev?schema=public

# Timeouts / Retries
OPENAI_TIMEOUT_MS=500
RETRIES=1

# E2E flags
E2E=1
NEXT_PUBLIC_E2E=1
PDF_RENDERER_FORCE=react
STORAGE_PROVIDER=file
RAG_OCR_ENABLED=1

